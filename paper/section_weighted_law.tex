\section{A weighted AGOP law from stationarity}
\label{sec:weighted-law}

We study a two-layer model
\[
f(x)=a^\top \phi(Bx),
\qquad
B\in\mathbb R^{m\times d},
\]
trained with square loss and \(L_2\)-regularization on \(B\):
\[
\mathcal L(B,a)
=
\frac{1}{2n}\sum_{i=1}^n (f(x_i)-y_i)^2
+
\frac{\lambda}{2}\|B\|_F^2.
\]
The classical neural feature ansatz motivates comparing the learned feature
matrix
\[
H:=B^\top B
\]
to an AGOP-type matrix. Our derivation and experiments suggest that the stable
late-training relation is not primarily a raw AGOP law. It is a
residual-weighted AGOP law.

For each training sample define
\[
r_i := f(x_i)-y_i,
\qquad
q_i := a\odot \phi'(Bx_i).
\]
Let
\[
X := [x_1,\ldots,x_n],
\qquad
Q := [q_1,\ldots,q_n],
\qquad
R := \operatorname{diag}(r_1,\ldots,r_n).
\]
Since \(\nabla f(x_i)=B^\top q_i\), the residual-weighted AGOP is
\[
\widetilde G
:=
B^\top Q R^2 Q^\top B
=
\sum_{i=1}^n r_i^2 \nabla f(x_i)\nabla f(x_i)^\top.
\]

\subsection{Stationarity identity}

The gradient with respect to \(B\) is
\[
\nabla_B\mathcal L
=
\frac1n QRX^\top + \lambda B.
\]
Define the stationarity defect
\[
Z := \lambda n B + QRX^\top.
\]
Then
\[
\lambda n B = -QRX^\top + Z.
\]
With
\[
S:=QRX^\top XRQ^\top,
\qquad
T:=QR^2Q^\top,
\]
we obtain
\[
BB^\top
=
\frac{1}{\lambda^2 n^2}S
+
E_{\mathrm{stat}},
\]
where
\[
E_{\mathrm{stat}}
:=
\frac{1}{\lambda^2 n^2}
\left(
-QRX^\top Z^\top
-
ZXRQ^\top
+
ZZ^\top
\right).
\]
At exact stationarity, \(Z=0\), and therefore
\[
BB^\top
=
\frac{1}{\lambda^2 n^2}S.
\]

\subsection{Effective dimension and pair diagnostics}

The naive isotropic closure would replace \(S\) by \(dT\). This is too crude
under anisotropic or low-rank input families. We instead use the best scalar
fit
\[
d_{\mathrm{eff}}
:=
\frac{\langle S,T\rangle_F}{\|T\|_F^2},
\qquad
\kappa_{\mathrm{eff}}
:=
\frac{d_{\mathrm{eff}}}{\lambda^2 n^2}.
\]
The resulting hidden-space closure is
\[
BB^\top \approx \kappa_{\mathrm{eff}} T.
\]
Pushing through \(B^\top(\cdot)B\) gives the weighted square law
\[
H^2
\approx
\kappa_{\mathrm{eff}}\widetilde G.
\]

If \(T\) is singular, let \(P_T\) be the projector onto \(\operatorname{range}(T)\).
The support-normalized pair diagnostic is
\[
\mathcal A_{\mathrm{pair}}
:=
\left\|
T^{\dagger/2}ST^{\dagger/2}
-
d_{\mathrm{eff}}P_T
\right\|_{\mathrm{op}}.
\]
This quantity is intentionally worst-case. It can be large because
\(T^{\dagger/2}\) amplifies directions with small nonzero support. For this
reason we also monitor the pushed-forward pair error
\[
\left\|B^\top(S-d_{\mathrm{eff}}T)B\right\|_{\mathrm{op}},
\]
which is closer to the error entering the final weighted law.

\subsection{High-gain pair closure}

The pushed diagnostic has an exact stationarity-coordinate form. Let
\[
A=QR=U\Sigma V^\top
\]
be the thin SVD of \(A\), and define
\[
H_X:=V^\top X^\top X V,
\qquad
F_X:=H_X-d_{\mathrm{eff}}I.
\]
Then
\[
T=U\Sigma^2U^\top,
\qquad
S-d_{\mathrm{eff}}T=U\Sigma F_X\Sigma U^\top.
\]
At exact \(B\)-stationarity,
\[
B=-\frac1{\lambda n}U\Sigma V^\top X^\top.
\]
Therefore
\[
B^\top(S-d_{\mathrm{eff}}T)B
=
\frac1{\lambda^2n^2}
G_{\mathrm{stat}}^\top F_XG_{\mathrm{stat}},
\qquad
G_{\mathrm{stat}}:=\Sigma^2V^\top X^\top.
\]
Thus the weighted law does not require \(F_X\) to be small on all of
\(\operatorname{range}(T)\). It only requires scalar closure on directions with
large stationarity-induced gain.

More precisely, let \(P_{\mathrm{hi}}\) be an orthogonal projector in
\(\mathbb R^r\). If
\[
\|P_{\mathrm{hi}}F_XP_{\mathrm{hi}}\|_{\mathrm{op}}\le \varepsilon,
\qquad
\|(I-P_{\mathrm{hi}})G_{\mathrm{stat}}\|_{\mathrm{op}}\le \delta,
\]
then
\[
\|B^\top(S-d_{\mathrm{eff}}T)B\|_{\mathrm{op}}
\le
\frac1{\lambda^2n^2}
\left[
\varepsilon\|G_{\mathrm{stat}}\|_{\mathrm{op}}^2
+
2\mathcal A_{\mathrm{pair}}\|G_{\mathrm{stat}}\|_{\mathrm{op}}\delta
+
\mathcal A_{\mathrm{pair}}\delta^2
\right].
\]
This is the high-gain version of pair closure tested by the spectral-gain
diagnostics.

For isotropic Gaussian inputs, high-gain scalar closure can be justified
conditionally by an adaptive concentration statement. If the learned high-gain
subspace \(W=VP_{\mathrm{hi}}\) were fixed independently of \(X\), then
\(W^\top X^\top XW\) would concentrate around \(dI\). For learned \(W\), the
same argument applies uniformly over any low-complexity class of possible
high-gain subspaces. Thus the remaining training-geometry assumption is that
regularized training selects a stable or low-complexity high-gain subspace.

\subsection{Deterministic weighted law}

Let
\[
E_{\mathrm{pair}}:=S-d_{\mathrm{eff}}T.
\]
Then
\[
H^2-\kappa_{\mathrm{eff}}\widetilde G
=
B^\top
\left(
\frac{1}{\lambda^2 n^2}E_{\mathrm{pair}}
+
E_{\mathrm{stat}}
\right)B.
\]
Taking operator norms gives
\[
\|H^2-\kappa_{\mathrm{eff}}\widetilde G\|_{\mathrm{op}}
\le
\|B\|_{\mathrm{op}}^2
\left(
\frac{\|T\|_{\mathrm{op}}}{\lambda^2 n^2}
\mathcal A_{\mathrm{pair}}
+
\|E_{\mathrm{stat}}\|_{\mathrm{op}}
\right).
\]
This is the deterministic theorem template tested by the experiments.

\subsection{Why the raw AGOP law becomes ill-conditioned}

The raw AGOP bridge compares
\[
M=\frac1n\sum_{i=1}^n q_iq_i^\top,
\qquad
\widetilde M=\frac1n\sum_{i=1}^n r_i^2q_iq_i^\top.
\]
Define the Frobenius best-fit scalar
\[
\beta_{\mathrm{fit}}
:=
\frac{\langle \widetilde M,M\rangle_F}{\|M\|_F^2}.
\]
Using
\[
\langle q_iq_i^\top,q_jq_j^\top\rangle_F
=
(q_i^\top q_j)^2,
\]
we get
\[
\beta_{\mathrm{fit}}
=
\frac{
\sum_{i,j} r_i^2(q_i^\top q_j)^2
}{
\sum_{i,j}(q_i^\top q_j)^2
}.
\]
Thus \(\beta_{\mathrm{fit}}\) is a nonnegative weighted average of the squared
residuals. In particular,
\[
\min_i r_i^2
\le
\beta_{\mathrm{fit}}
\le
\max_i r_i^2.
\]
As the model approaches interpolation, \(\beta_{\mathrm{fit}}\to 0\). Converting
the weighted law into a raw AGOP law then requires division by a small scalar,
which makes the raw relation ill-conditioned late in training.

The same identity gives a sharper beta diagnostic. Define
\[
s_i:=\sum_{j=1}^n(q_i^\top q_j)^2,
\qquad
\ell_i:=\frac{s_i}{\frac1n\sum_k s_k},
\qquad
\bar r^2:=\frac1n\sum_i r_i^2.
\]
Then
\[
\frac1n\sum_i\ell_i=1,
\qquad
\beta_{\mathrm{fit}}=\frac1n\sum_i\ell_i r_i^2,
\]
and, when the empirical variances are nonzero,
\[
\frac{\beta_{\mathrm{fit}}}{\bar r^2}-1
=
\operatorname{Corr}_n(\ell,r^2)
\operatorname{CV}_n(\ell)
\operatorname{CV}_n(r^2).
\]
Thus beta tracks mean residual energy when hidden-gradient leverage and squared
residuals have small normalized covariance.

There are clean conditional sufficient conditions for this decorrelation. If,
conditional on the hidden-gradient geometry,
\[
\mathbb E[r_i^2\mid q_1,\ldots,q_n]=\mu
\qquad
\text{for all }i,
\]
then
\[
\mathbb E[\beta_{\mathrm{fit}}\mid q_1,\ldots,q_n]
=
\mathbb E[\bar r^2\mid q_1,\ldots,q_n].
\]
In population form, a sufficient condition is
\[
\operatorname{Cov}(r(X)^2,h(X))=0,
\qquad
h(x):=\mathbb E_{X'}[(q(x)^\top q(X'))^2].
\]

One possible dynamics explanation is leverage-sensitive damping. In the
simplified model
\[
\dot r_i(t)=-(\eta_0+\alpha(\ell_i-1))r_i(t),
\qquad
\alpha>0,
\]
positive covariance between \(\ell_i\) and \(r_i(t)^2\) is suppressed. For the
full two-layer network this remains a bridge assumption, because the residual
dynamics are governed by the NTK and the leverage scores also move during
training.

The obstruction is concrete. The NTK contains the terms
\[
K^{\mathrm{NTK}}_{ij}
=
\phi(Bx_i)^\top\phi(Bx_j)
+
(q_i^\top q_j)(x_i^\top x_j),
\]
while beta leverage is proportional to
\[
\sum_j(q_i^\top q_j)^2.
\]
These quantities need not order samples in the same way. A clean conditional
dynamics model is
\[
\dot r=-(\eta_0 I+\alpha D_\ell+E)r,
\qquad
D_\ell:=\operatorname{diag}(\ell_i-1),
\qquad
\alpha>0.
\]
In this model the covariance damping survives when the perturbation \(E\) is
small enough relative to the leverage-weighted residual component. Proving an
approximation of this form for the true NTK dynamics remains open.

\subsection{Empirical summary}

We test isotropic Gaussian, anisotropic Gaussian, and low-rank signal plus
isotropic noise families. Across all three families, the observed weighted
residual is small at best-stationarity checkpoints and the deterministic bound
is conservative on average. The raw support-normalized \(\mathcal A_{\mathrm{pair}}\)
can be large, but the pushed-forward pair error is small. Across all logged
checkpoints, \(\beta_{\mathrm{fit}}\) closely tracks mean residual squared,
supporting the interpretation that the raw-to-weighted bridge scalar is
essentially training residual energy in these experiments.
